import tunnel from "tunnel-rat";

// default layer, web
export const ui = tunnel();

// immediate mode GUI, webgl ctx
export const im = tunnel();

// Geospatial shader for TSL
// export const geo = tunnel()

// Enhanced Interaction Events, Prioritize large screen and keyboard, with mouse interactions rewritten.
// export const cmd = tunnel()
